# 操作の練習

読むだけでは指が覚えない。**実際にキーを押して壊しながら覚える**ための教材。

関連: [Vim / Neovim 入門](vim.md)（解説） ／ [tmux](tmux.md) ／ [ウィンドウ操作](window.md)

---

## 始め方

Neovim の中で次を打つ。

```
:Practice
```

一覧が出るので選ぶ。番号を直接指定してもよい。

```
:Practice 03
```

**開かれるのは複製です。** 原本はこのリポジトリに残るので、どれだけ壊してもリポジトリに差分は出ない。初期状態に戻すには `:Practice` をもう一度実行する。

| 操作 | 意味 |
|---|---|
| `u` | 1手戻す |
| `:Practice` | その教材を初期状態から開き直す |
| `:e!` | 保存せず開き直す |

---

## 教材

| | 内容 | 主に扱うもの |
|---|---|---|
| [01 基本操作](practice/01-basics.md) | 開く・分ける・移る | `<Space>ff` `<Space>\` `Ctrl+h/j/k/l` `]b` `[b` |
| [02 ノーマルモード](practice/02-normal.md) | 動詞 + 移動 | `dw` `ct)` `dd` `.` `3w` |
| [03 挿入モード](practice/03-insert.md) | どこから打ち始めるか | `i` `a` `I` `A` `o` `O` `jk` `Ctrl+n` |
| [04 ビジュアルモード](practice/04-visual.md) | 選んでから決める | `v` `V` `Ctrl+v` `J` `K` `gv` |
| [05 テキストオブジェクト](practice/05-textobject.md) | 構造で範囲を指定する | `di{` `ci"` `daa` `daf` `ysiw"` |
| [06 コマンドライン](practice/06-cmdline.md) | まとめて直す | `*` `cgn` `:s` `:g` `:cfdo` |

**上から順にやる。** 02 の `.` が身につく前に 06 の `cgn` へ進んでも効かない。

---

## 進め方

各教材は「課題 → 例のコード → 打鍵 → 確認」の形になっている。

1. 課題を読む
2. **例のコードの上で実際にキーを押す**
3. 想定通りにならなければ `u` で戻してやり直す
4. 分からなければ `<details>` の中の手順を開く

1つの教材は10〜15分ぶん。**全部を一度にやらない。** 1日1つで6日、それを2周する方が身につく。

---

## 教材を足す

`docs/practice/` に `07-*.md` のように置けば、`:Practice` の一覧に自動で出る。ファイル名の番号順に並ぶ。

1行目の `# ...` が一覧に表示される名前になる。
